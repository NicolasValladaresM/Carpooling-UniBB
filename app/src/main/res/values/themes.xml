<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Prueba" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/dark_blue</item>
        <item name="android:textColorPrimary">@android:color/white</item> <!-- Adjust the text color here -->

        <!-- Define your own colors for dark mode -->
        <item name="colorPrimaryDark">@color/dark_blue</item>

        <!-- Specify a different color for text in dark mode -->
        <item name="android:textColorSecondary">@color/white</item>

        <!-- Disable automatic day/night mode color changing -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>

    <style name="Theme.Prueba" parent="Base.Theme.Prueba">
        <!-- You may customize additional styles here if needed -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
